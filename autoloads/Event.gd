extends Node

signal ambient(action: bool)

signal show_menu(action: bool)

signal change_state()

signal show_visor(action: bool)

signal change_visor_size(new_size: Vector2)

signal start_display_animation(message: String, audio_path: String)

signal end_display_animation()

signal scorpus_end()
